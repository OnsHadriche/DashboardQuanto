// project imports
import Navbar from 'layout/DefaultLayout/NavBar';

// ==============================|| AUTHENTICATION ROUTING ||============================== //

const DefaultRoutes = {
  path: '/',
  element: <Navbar />
};

export default DefaultRoutes;
